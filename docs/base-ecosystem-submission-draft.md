# Base ecosystem listing — copy-paste draft

Prepared 2026-08-02. Submit via the official form: **https://forms.gle/hJhc2PqfAsQp86YL8**

**Pull requests to `base/web` are no longer accepted** for ecosystem listings — the form is
the only route. Any older note saying otherwise is stale.

Why it's worth doing: it is the official Base directory, so it delivers discovery *and* a
legitimacy signal at once — the same signal that reduces "unknown site" wallet warnings.

---

## Fields

| Field | Value |
| --- | --- |
| Project name | BasePulse |
| Website | `https://basepulse.botsniper.xyz` |
| Category | Tools / Infrastructure (portfolio & analytics utility) |
| Live on Base mainnet | Yes |
| Contract | `0xd2240b90486F63858ED823a2620cb6DC6FcB6019` (verified) |
| Builder Code | `bc_bo6g6vzn` |
| Farcaster | `@usebasepulse` |
| Contact | hersonc00@proton.me |
| Logo | `docs/assets/dappradar/logo-250.png` (250×250 PNG, 25KB) |
| Screenshots | `docs/assets/dappradar/screenshot-{1-home,2-score,3-gas}.png` |

## Short description (one line)

> Portfolio, gas timing, and onchain score for Base — look up any wallet, no account needed.

## Full description

> BasePulse is a free utility dashboard for Base. Paste any Base wallet address to see its
> token portfolio, a gas-fee tracker with 7-day historical guidance on when fees are low,
> and an onchain reputation score broken down by what drives it — Base activity, token
> diversity, L1 history and bridging. Every score page produces a shareable card for X and
> Farcaster. No signup and no wallet connection are needed to look up an address; connect
> only to view your own portfolio or to seal a snapshot of your holdings onchain — a
> keccak256 fingerprint, never the amounts.

## Positioning note (useful context, not a form field)

Base's 2026 pivot named **trading, payments and AI agents** as its three pillars and shut
down the social/creator pillar. BasePulse is a trading-utility dashboard, so it sits in a
pillar that is currently a priority rather than one that was just discontinued. Worth
leaning on if the form has a free-text "why does this fit Base" field.

## Do before submitting

1. **Post something from `@usebasepulse`.** The account exists but is empty; an ecosystem
   listing pointing at a profile with zero posts is a weak signal. The share cards work as
   of 27-jul, so posting a score card is a decent first post.
2. Sanity-check the three screenshots still match the live UI — the portfolio page changed
   on 2026-08-02 (the onchain snapshot block is new and now renders for signed-out
   visitors), so `screenshot-1-home.png` may be stale.
